# Smart-MLFQ workload: three_way
# Each MLFQ queue gets a candidate that runs long enough for demotion +
# aging to fire visibly in the trace. Tuned for the project's QEMU speed
# (~100 ticks/sec); cpu_burner 20M iters ≈ 50–100 ticks, enough to walk
# all three queue levels.
#   cpu_burner   → demotes to LOW under standard MLFQ rules
#   mixed_burner → settles in MID  (CPU bursts interleaved with sleep)
#   io_burner    → stays in HIGH  (sleep-heavy, I/O-aware retention)
cpu_burner   20000000 0
mixed_burner 80       0
io_burner    40       1
