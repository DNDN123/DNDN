# 스레드 파트 구현 문서
**담당자:** 안진환 | **파트:** Thread / Synchronization  
**운영체제:** xv6-riscv | **연동:** Solar API (solar_bridge.py)

---

## 1. 전체 구조 요약

```
[유저 자연어 입력]
        ↓
[solar_bridge.py]  ──→  Solar API (번역만)
        ↓ xv6 명령어
[xv6 QEMU]
  ├── thread_create / thread_join / thread_exit   ← 커널 syscall
  ├── futex_wait / futex_wake                     ← 커널 syscall
  ├── mutex_t / condvar_t                         ← 유저 라이브러리
  └── threadtest                                  ← 테스트 프로그램
```

---

## 2. 구현 파일 목록

### xv6 커널 수정 파일

| 파일 | 수정 내용 |
|------|-----------|
| `kernel/syscall.h` | syscall 번호 5개 추가 (22~26번) |
| `kernel/proc.h` | `struct proc`에 `is_thread`, `tgroup` 필드 추가 |
| `kernel/proc.c` | `kthread_create()`, `kthread_join()`, `kfutex_wait()`, `kfutex_wake()` 구현, `freeproc()` 수정 |
| `kernel/vm.c` | `uvmshare()`, `thread_freepagetable()` 구현 |
| `kernel/sysproc.c` | `sys_thread_create/join/exit`, `sys_futex_wait/wake` 구현 |
| `kernel/syscall.c` | syscall 테이블에 새 항목 등록 |
| `kernel/defs.h` | 새 함수 선언 추가 |

### xv6 유저 영역 파일

| 파일 | 수정/신규 | 내용 |
|------|-----------|------|
| `user/usys.pl` | 수정 | thread/futex syscall 스텁 추가 |
| `user/user.h` | 수정 | API 함수 선언 추가 |
| `user/umutex.h` | **신규** | mutex_t, condvar_t 구현 |
| `user/threadtest.c` | **신규** | 4가지 테스트 프로그램 |

### 호스트 파일

| 파일 | 내용 |
|------|------|
| `solar_bridge.py` | Solar API 연동 + xv6 자동 실행 브릿지 |

---

## 3. 시스템 콜 목록

| 번호 | 이름 | 인자 | 반환 | 설명 |
|------|------|------|------|------|
| 22 | `thread_create` | fn, arg, stack | tid / -1 | 스레드 생성 |
| 23 | `thread_join` | tid | 0 / -1 | 특정 스레드 종료 대기 |
| 24 | `thread_exit` | - | (없음) | 현재 스레드 종료 |
| 25 | `futex_wait` | addr, expected | 0 / -1 | 조건부 슬립 |
| 26 | `futex_wake` | addr | 0 / -1 | 슬립 스레드 깨우기 |

---

## 4. 핵심 구현 설명

### 4-1. thread_create — VM 공유 스레드 생성

**핵심 아이디어:** xv6의 `fork()`는 부모의 메모리를 **복사**하지만,  
스레드는 같은 메모리를 **공유**해야 함 → `uvmshare()` 직접 구현

```
fork()        : 부모 페이지 → 새 물리 페이지로 복사 (독립된 메모리)
thread_create : 부모 페이지 → 같은 물리 페이지를 그대로 매핑 (공유 메모리)
```

**구현 흐름:**
```
thread_create(fn, arg, stack)
  ↓
allocproc()          // 새 proc 구조체 할당 (커널 스택, trapframe 별도)
  ↓
uvmshare()           // 부모 페이지테이블 순회 → 같은 물리 주소로 매핑
  ↓
trapframe 설정
  epc = fn           // 스레드 시작 주소
  sp  = stack        // 유저가 할당한 스택 (malloc으로 미리 확보)
  a0  = arg          // 첫 번째 인자
  ↓
is_thread = 1        // 나중에 freeproc이 공유 메모리를 해제하지 않도록 표시
  ↓
state = RUNNABLE     // 스케줄러가 실행 시작
```

**스레드 종료 시 메모리 처리 (freeproc 수정):**
```
일반 프로세스: proc_freepagetable() → 물리 페이지까지 모두 해제
스레드:       thread_freepagetable() → 매핑(페이지테이블 구조)만 해제
                                        물리 페이지는 부모가 소유 → 건드리지 않음
```

---

### 4-2. futex_wait / futex_wake — Lost Wakeup 없는 동기화

**문제:** 뮤텍스 해제와 슬립 사이에 다른 스레드가 시그널을 보내면 놓침

```
[스레드 A]              [스레드 B]
락 확인 (잠김)
                        락 해제
                        wakeup (아무도 안 자고 있음 → 유실!)
sleep() 진입 ← 여기서 영원히 못 깸
```

**해결:** `futex_lock`(전역 스핀락)으로 "값 확인 → 슬립" 구간을 원자적으로 보호

```c
// kernel/proc.c
int kfutex_wait(uint64 uaddr, int expected) {
    acquire(&futex_lock);
    copyin(...)           // 현재 값 읽기
    if (val != expected)  // 이미 바뀌었으면 바로 리턴 (lost wakeup 방지)
        { release(&futex_lock); return 0; }
    sleep(uaddr, &futex_lock);  // 원자적으로 락 해제 + 슬립
    release(&futex_lock);
}

int kfutex_wake(uint64 uaddr) {
    acquire(&futex_lock);
    wakeup(uaddr);        // 해당 주소에서 자는 스레드 모두 깨움
    release(&futex_lock);
}
```

---

### 4-3. mutex_t — 유저 공간 뮤텍스

**빠른 경로 (경쟁 없음):** CAS 한 번으로 syscall 없이 획득  
**느린 경로 (경쟁 있음):** `futex_wait`으로 커널에서 슬립 → CPU 낭비 없음

```c
// user/umutex.h
void mutex_lock(mutex_t *m) {
    while (__sync_val_compare_and_swap(&m->state, 0, 1) != 0)
        futex_wait(&m->state, 1);  // 잠겨있으면 커널에서 재움
}

void mutex_unlock(mutex_t *m) {
    __sync_lock_release(&m->state);  // 원자적으로 0으로 설정
    futex_wake(&m->state);           // 대기 스레드 깨움
}
```

---

### 4-4. condvar_t — 조건 변수

**아이디어:** 시퀀스 번호를 futex 워드로 사용  
signal/broadcast 때 번호를 올리면, wait이 이미 바뀐 번호를 보고 즉시 리턴 → lost wakeup 없음

```c
void condvar_wait(condvar_t *cv, mutex_t *m) {
    int seq = cv->seq;        // 시그널 전 번호 저장
    mutex_unlock(m);
    futex_wait(&cv->seq, seq); // seq가 바뀌면 즉시 리턴 (lost wakeup 방지)
    mutex_lock(m);
}

void condvar_signal(condvar_t *cv) {
    __sync_fetch_and_add(&cv->seq, 1);  // 번호 올리기
    futex_wake(&cv->seq);
}
```

---

### 4-5. Solar API 브릿지

**구조:**
```
[유저] 자연어 입력
  ↓
solar_bridge.py
  ↓ HTTP POST
Solar API (solar-pro 모델)
  ↓ xv6 명령어 반환
solar_bridge.py
  ↓ QEMU stdin에 전송
xv6 셸이 명령 실행
  ↓
결과 출력 → 유저에게 표시
```

**Solar API 역할:** 자연어 해석 및 명령 번역만 담당  
**실행 및 관리:** xv6 OS 레이어가 직접 담당 (LLM 래퍼가 아님)

---

## 5. 테스트 결과 (threadtest)

| 테스트 | 내용 | 기대값 | 확인 |
|--------|------|--------|------|
| test 1 | 스레드 2개가 공유 카운터 각 100,000번 증가 | 200,000 | ✅ |
| test 2 | 스레드 4개 생성 후 join | 모두 정상 출력 | ✅ |
| test 3 | mutex로 보호된 카운터 4스레드 × 50,000 | 200,000 | ✅ |
| test 4 | condvar 기반 producer/consumer | sum = 499,500 | ✅ |

---

## 6. 적용된 OS 개념 정리

| OS 개념 | 구현 방식 |
|---------|-----------|
| **스레드** | xv6 proc 구조 재활용, 페이지테이블 공유 |
| **가상 메모리** | uvmshare() — 같은 물리 프레임을 두 페이지테이블에 매핑 |
| **시스템 콜** | 5개 신규 추가 (22~26번), syscall 테이블 등록 |
| **동기화 — mutex** | CAS + futex_wait (2단계: 빠른/느린 경로) |
| **동기화 — condvar** | 시퀀스 번호 + futex, lost wakeup 완전 방지 |
| **스케줄링** | xv6 기존 Round-Robin 스케줄러 활용 |
| **LLM 통합** | Solar API = 번역기, xv6 = 실행기 (역할 분리) |

---

## 7. 발표 대본 포인트

> **"저는 xv6 커널을 직접 수정해서 스레드와 동기화를 구현했습니다."**

1. xv6에는 원래 스레드가 없습니다. fork()만 있고 메모리를 복사합니다.
2. 스레드는 메모리를 공유해야 하므로, `uvmshare()`라는 함수를 새로 만들어 같은 물리 페이지를 두 페이지테이블에 매핑했습니다.
3. 동기화를 위해 Linux의 futex와 동일한 원리로 `futex_wait/wake` syscall을 xv6 커널에 추가했습니다.
4. 이 위에 mutex와 조건변수를 구현해 스레드 간 안전한 자원 공유를 가능하게 했습니다.
5. Solar API는 자연어를 xv6 명령어로 번역하는 역할만 하고, 실제 실행은 우리가 만든 OS 레이어가 담당합니다.

---

*작성일: 2026-05-20*
